class Test{
    //no parameterised constructor  (no need to call the constructor its a special function which executes when you create object)
    constructor(){
        console.log("vimal")
    }
    //function- we need to call the function with help of object 
    show(){
      console.log("harshad")
    }
}